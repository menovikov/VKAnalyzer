<Window x:Class="VKAnalyzer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MainWindow" Height="350" Width="525">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Button Content="Get groups" Click="Button_Click" Margin="0,0,0,31"/>
        <Button Content="Try to compare groups" Click="Button_Click1" Grid.Row="2" Margin="0,39,0,0"/>
        <TextBlock Name="ListOfGroups" Grid.Row="1" Margin="0,0,416,0"/>
        <ListBox x:Name="ListBox" HorizontalAlignment="Left" Height="176" Margin="157,75,0,0" Grid.RowSpan="3" VerticalAlignment="Top" Width="360" />
    </Grid>
</Window>
